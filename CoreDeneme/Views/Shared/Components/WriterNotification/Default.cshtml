@using EntityLayer.Concrete
@model List<Notificition>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0">Bildirimler</h6>
    @foreach(var x in Model)
    {
    <div class="dropdown-divider"></div>
    <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">
            <div class="@x.NotificitionColor">
                <i class="@x.NotificitionTypeSymbol"></i>
            </div>
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
            <h6 class="preview-subject font-weight-normal mb-1">@x.NotificitionType </h6>
            <p class="text-gray ellipsis mb-0"> @x.NotificitionDetails </p>
        </div>
    </a>
    }
   
    <div class="dropdown-divider"></div>
    <h6 class="p-3 mb-0 text-center">Tüm Bildirimleri Gör  </h6>
</div>